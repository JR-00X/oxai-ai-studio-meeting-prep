# sample_outputs/

Structured JSON responses returned by the Gem for each prompt iteration (`v1.json`, `v2.json`, `v3.json`). Kept alongside `/prompts/` so diffs between iterations can be read against concrete model behaviour, not just prompt text.
