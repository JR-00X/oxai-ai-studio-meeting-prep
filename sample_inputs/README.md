# sample_inputs/

Fictional Acme Corp QBR inputs used to exercise the Gem: `notes.pdf` (account manager's call notes) and `slides.pptx` (the QBR presentation deck). These are the canonical test inputs referenced by every prompt iteration in `/prompts/`.
