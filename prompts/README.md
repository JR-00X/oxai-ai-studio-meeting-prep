# prompts/

Full text of every prompt iteration run in Google AI Studio — one file per version (`v1.md`, `v2.md`, `v3.md`). Each file contains the exact system/user prompt pasted into the Gem so the iteration history is reproducible outside AI Studio.
